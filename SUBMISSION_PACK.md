# Submission Pack — Socratic Code Tutor

Drafts only. Nothing in this file is published, posted, or scheduled — every asset below is
prepared for a human to review, fill in the bracketed placeholders, and publish manually.
Facts are anchored to `README.md`, `REQUIREMENTS.md` §1/§5, `UX-SPEC.md`, `PLAN.md`, and
`FIX_ROUND_1.md`. Where a number or link isn't yet finalized in the repo, it's marked
`[unverified — confirm before submitting]` rather than invented.

Tagline used throughout: **"Debug it yourself. We'll only ask questions."** (from `README.md`.)

---

## 1. Demo video script (target 2:40, hard ceiling 3:00)

Recording note: tutor phrasing in beats 2–3 is **live GPT-5.6 output**, not scripted — the
voiceover lines are what the narrator says over the screen capture; the on-screen chat text will
vary run to run. Rehearse once beforehand so the actual escalation lands within the timing below,
but do not fake or pre-script the model's words.

### Recording setup checklist (do this before hitting record)
- [ ] Reset to a clean session: clear `localStorage` (or open a fresh profile/incognito window) so the demo starts from zero sessions — no leftover chat history or stale teacher-view data.
- [ ] Browser window sized to **1440px** wide (matches the primary UX-SPEC breakpoint; avoids the 768px collapsed layout).
- [ ] Hide the bookmarks bar and any extensions toolbar clutter; close unrelated tabs.
- [ ] `prefers-reduced-motion`: **OFF** for this recording — the staircase hint-ladder animation and streaming cursor are part of the "authored, not templated" story and should be visible.
- [ ] Confirm the deployed Vercel URL is live and responds in under 5s before recording (per SC-3).
- [ ] Pre-warm Pyodide once off-camera (load the page, let the ~10MB runtime cache) so the on-camera load isn't spent watching a cold-start progress bar — unless you want to show the "Booting a real Python interpreter" loading state deliberately (optional, cut for time in the 2:40 target).
- [ ] Have the **teacher view** pre-seeded with 2 additional sample sessions (beyond the one you'll run live) so the misconception bar chart shows ≥3 sessions per SC-6, without live-recording three full runs.
- [ ] Silence or an original/royalty-free instrumental only — no third-party or copyrighted music per the submission rules.

### Beat-by-beat script

**0:00–0:18 — Hook: the problem**
*On screen:* Landing screen, `DemoModeBanner`, then a quick cut to the code editor with the
`off_by_one` sample already visible (not yet run).
*Voiceover:*
> "When a student's code breaks, the fastest help — a chatbot, a forum post — usually just hands them a fixed line. The bug gets solved. The understanding doesn't. Office hours don't scale to every 'why is this broken,' so most of those questions get answered by whoever's fastest, not whoever teaches best. This is Socratic Code Tutor: it never gives you the answer. It asks the question that gets you there yourself."

**0:18–0:45 — Live demo: run the broken sample**
*On screen:* Click **Sample Library**, choose "One step too far" (off-by-one, `IndexError`).
Click **Run**. Show the structured `TracebackCard` — exception type, message, line number — appear
in the Output tab.
*Voiceover:*
> "This runs for real — Python, compiled and executed with Pyodide, entirely in the browser. No student code ever touches our server. It fails with an IndexError on line 4. A normal assistant would just tell me to change `nums[i+1]` to `nums[i]`. Watch what this one does instead."

**0:45–1:30 — The hint ladder: escalate, and it still won't give the answer**
*On screen:* Tutor's first message appears in `TutorChatPanel` (a conceptual question, not a fix).
Click **"I'm stuck — hint"** to climb the `HintLadderRail` staircase — capture the rung indicator
changing ("Hint 1 of 4" → "Hint 4 of 4") and the visible lit-rung animation. Show the tutor's rung-4
response on screen and let it sit for 2–3 seconds so the viewer can read that it's still prose, not
code.
*Voiceover:*
> "Every reply is generated by GPT-5.6, but it's structurally blocked from ever handing over a fix. There's a visible four-rung ladder — the student chooses to escalate, one rung at a time: a conceptual question, then localization, then naming the mechanism, and at rung four, the ceiling — a near-solution description, still not a runnable line. Every model response is also screened server-side, after generation and before it ever reaches this chat, so even if the model drifted, nothing unscreened gets shown."

**1:30–1:50 — Execution trace: see the real runtime, not a guess**
*On screen:* Switch to the **Trace** tab. Scrub the `TraceScrubber` step by step; show the
`VariableTable` values for `s`, `i`, `nums` changing per step, and the call-stack badge.
*Voiceover:*
> "This trace isn't simulated — it's captured live from Python's own `sys.settrace` inside the Pyodide sandbox. The student can step through exactly what their variables actually did, and compare that against what they expected."

**1:50–2:10 — Teacher view: aggregate misconceptions, not just pass/fail**
*On screen:* Navigate to **Teacher view**. Show the misconception bar chart with the pre-seeded
sessions (off-by-one, mutation-vs-copy, etc.) sorted by count. Click **Export**, show the
downloaded file.
*Voiceover:*
> "Every session gets tagged with a misconception category by GPT-5.6 — off-by-one, mutation versus copy, scope confusion — and a teacher can see the pattern across a whole class, on one screen, from sessions stored right in the browser. No backend, no login. Export it as JSON or Markdown and take it into office hours."

**2:10–2:35 — How this was built**
*On screen:* Quick cut to a terminal or code view — the Codex session transcript or commit log
(e.g., `git log --oneline`), then a glance at the buffered-guardrail code in
`app/api/tutor/route.ts` or the `guardrail.ts` file.
*Voiceover:*
> "The product contract and UX spec were written up front; Codex, driving GPT-5.6, then implemented the core application in one continuous session — the Pyodide runner, the trace capture, the buffered guardrail pipeline, the hint ladder, and the teacher export. A four-part agency audit — TypeScript, React, security, and solution-leak verification — found real guardrail bypasses in round one. Codex fixed every one of them in a single follow-up pass, and the test count only went up. GPT-5.6 is doing two jobs here: it's the model Codex used to write the code, and it's the model running the tutor and the misconception tagging live, in production, right now."

**2:35–2:40 — Close**
*On screen:* Landing screen, URL text card, Education track badge.
*Voiceover:*
> "Socratic Code Tutor — Education track, OpenAI Build Week. Link's below."

**Fallback if something breaks live:** Keep a second browser tab open with a session already run
and the trace/teacher-view pre-populated. If the live Pyodide run stalls or the API rate-limits
mid-recording, cut to the pre-populated tab and narrate over it ("here's a run captured just
before this recording") rather than stopping the take. Do not re-record the whole video for a
single flaky beat — cut around it in post.

---

## 2. Devpost submission text

**Project name:** Socratic Code Tutor
**Tagline:** Debug it yourself. We'll only ask questions.

### Inspiration
Office hours and TA time don't scale with class size, so most "why is my code broken" questions
get answered by whatever's fastest — a friend, a forum, a general chatbot — and the fastest answer
is almost always a corrected snippet. That's precisely the moment where the real learning happens:
forming and correcting a mental model of what the program is actually doing. Answer-first help
skips it, and the same misconception shows up again on the next assignment. We wanted a tutor that
could see what the code actually does at runtime and was structurally incapable of just fixing it
for you.

### What it does
A student pastes or picks a buggy Python sample, runs it against a real Pyodide interpreter in the
browser, and gets a structured traceback plus a step-through execution trace of real variable
state. GPT-5.6 tutors them through a visible four-rung hint ladder — conceptual question,
localization, mechanism, near-solution scaffold — that never reaches "here's the fixed code." Every
model reply is screened server-side, after generation and before delivery, so the no-solution rule
holds even if a reply drifts. Each session gets tagged with a misconception category, and a teacher
view aggregates those tags across a class with export to JSON/Markdown. No login, no database —
everything runs from the browser and Vercel.

### How we built it
The contract, UX spec, and trust-zone plan were written before any code. OpenAI Codex, driving
GPT-5.6, then implemented the core application from a work-package playbook in one continuous
primary session covering the sandboxed Pyodide runner, the prompt builder, the server-buffered
progressive-reveal guardrail pipeline, the hint ladder, rate/cost caps, the trace visualizer, and
the teacher-report/export path. GPT-5.6 does double duty in the finished product: it's the model
that generates every tutor turn and misconception tag at runtime, streamed via the OpenAI SDK
directly (not an abstraction layer, so the guardrail can sit between the model and the client).
Student code never leaves the browser; it executes only inside a Pyodide Web Worker. Model output
is buffered server-side and run through a deterministic screen at each sentence or code-fence
boundary — a flagged chunk halts the flush and substitutes a safe fallback question, so nothing
unscreened is ever shown.

### Challenges
The guardrail is the hardest part of this build precisely because "never show a fix" has to hold
against adversarial phrasing, not just the obvious case. A consolidated four-part agency review
(TypeScript correctness, React/streaming lifecycle, a security/STRIDE pass, and manual solution-leak
verification) found real bypasses — inline single-backtick code spans and gerund-phrased
instructions ("try changing line 4 to...") were slipping past rung-2 and rung-4 checks, and
per-chunk screening couldn't catch a fix split across streaming boundaries. Codex fixed the
detector, made screening cumulative across the whole turn instead of per-fragment, and added
integration tests that drive the real streaming pipeline — all in one follow-up pass, with the
test count strictly increasing.

### Accomplishments
A guardrail that's been adversarially tested against solution-shaped and injection-style prompts
with zero runnable fixes reaching the client fixture; an execution trace that reflects real Pyodide
`sys.settrace` output rather than a simulated one; and a teacher view that turns individual
sessions into an aggregate, actionable pattern — all with no backend database.

### What we learned
Streaming and "never reveal too early" are in direct tension — you cannot un-show a token. Solving
that meant giving up naive first-token latency in exchange for a hard "nothing unscreened reaches
the client" invariant, and testing that invariant against the actual streaming boundaries, not just
a full-string check after the fact.

### What's next
JavaScript execution alongside Python, a model-graded second guardrail layer for defense in depth,
and multi-device teacher aggregation beyond the current single-browser scope.

**Built with:** Next.js, TypeScript, Vercel, OpenAI GPT-5.6, OpenAI Codex, Pyodide, WebAssembly,
CodeMirror 6, Zustand, Vitest, Playwright

---

## 3. Social copy (drafts only — not posted)

### LinkedIn post (~120 words)

> Most "AI help" for student coders optimizes for one thing: making the error go away. That's the
> opposite of what a debugging moment is for.
>
> I spent Build Week on Socratic Code Tutor: paste failing Python, it runs for real in the browser
> (Pyodide, no server execution), and GPT-5.6 tutors through a visible four-rung hint ladder that
> is structurally blocked from ever handing over a fix — every reply is screened server-side before
> it's shown, not just prompted to behave. A live execution trace shows real variable state per
> step, and a teacher view aggregates what a whole class is actually misunderstanding.
>
> Built with OpenAI Codex driving GPT-5.6 end to end, including a fix round after an internal
> security/guardrail audit surfaced real bypasses.
>
> Demo + repo in the comments. Feedback welcome, especially from anyone who teaches intro CS.

### X / Twitter thread (3 tweets)

**Tweet 1 (hook):**
> Most AI coding help optimizes for making the error disappear. That skips the exact moment where
> debugging teaches you something.
>
> Built Socratic Code Tutor for OpenAI Build Week — it runs your broken Python for real, and
> refuses to give you the fix. 🧵

**Tweet 2 (mechanism):**
> GPT-5.6 tutors through a 4-rung hint ladder (question → localize → name the mechanism →
> near-solution scaffold) with a hard server-side screen on every reply before it's shown —
> not just a prompt asking nicely. Plus a real `sys.settrace` execution trace, not a simulated one.

**Tweet 3 (proof + CTA):**
> Built with OpenAI Codex driving GPT-5.6 for the full core build, including one fix round after an
> internal 4-part audit (TypeScript/React/security/solution-leak) found real guardrail bypasses —
> all closed, tests only went up.
>
> Live demo + repo: [link] — Education track, OpenAI Build Week.

### Short variants

- **X short hook / video caption:** "An AI tutor that runs your broken code for real — and is
  structurally blocked from ever showing you the fix."
- **Video thumbnail text idea:** "It won't give you the answer." *(pairs with the on-screen rung-4
  hint moment.)*

---

## Placeholders requiring the human before submission (`[unverified]`)

- `[Vercel deployment URL]` — not yet confirmed live at time of writing; DEPLOYMENT_CONTEXT.md
  smoke-test checklist is still open.
- `[GitHub repo URL]` — confirm public visibility + MIT license file is present before pasting
  into Devpost/LinkedIn/X.
- `[Codex /feedback session ID]` — README currently has this as `TODO before submission`; must be
  filled in both the README and the Devpost form before this pack's claims about "one continuous
  Codex session" are fully checkable by judges.
- `[YouTube URL]` — record per the script above, set visibility to public, then paste into Devpost.

No specific test-run numbers (coverage %, pass counts, latency ms) beyond what's already published
in `README.md`'s "Verification snapshot" were introduced in this pack — reuse that section verbatim
if a judge-facing summary needs a number, rather than re-measuring and risking drift from the
committed snapshot.
